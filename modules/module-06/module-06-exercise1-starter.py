#!/usr/bin/env python3
"""
Task Management System - Main Entry Point

This is the starting point for your task management application.
Use GitHub Copilot to help you build a complete multi-file system.

Hints:
- Start by implementing the Task model in src/models/task.py
- Then create the storage layer in src/storage/json_storage.py
- Build the service layer in src/services/task_service.py
- Finally, implement the CLI in src/cli.py
"""

import sys
from pathlib import Path

# Add src to Python path
sys.path.insert(0, str(Path(__file__).parent))


def main():
    """Main entry point for the task management system."""
    # TODO: Import and run the CLI
    # Copilot Prompt: Import the cli module and call the main function
    pass


if __name__ == "__main__":
    main()
